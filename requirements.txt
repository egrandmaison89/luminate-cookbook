# DFMC Email Banner Processor Dependencies
# Install with: pip install -r requirements.txt

# Web App
streamlit>=1.28.0

# Image Processing
Pillow>=10.0.0
opencv-python-headless>=4.8.0
numpy>=1.24.0

# For local CLI script (process_banners.py)
# These are also included above, so no additional installs needed

# Browser automation for Luminate uploads
# Note: Playwright Python package installs via pip, but browsers must be installed separately
# The app handles missing Playwright gracefully - it will show an error message instead of crashing
# If Playwright installation fails on Streamlit Cloud, the app will still start and show a helpful error message
playwright>=1.40.0; python_version >= "3.8"
python-dotenv>=1.0.0
requests>=2.28.0

# Optional: For persistent session storage on Google Cloud Run
# Uncomment if deploying to Cloud Run and want sessions to persist across instances
# google-cloud-storage>=2.10.0

